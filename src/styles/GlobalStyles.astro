<style lang="scss" is:global>
    :root {
        --backgroundColor: 6, 6, 6;
        --primaryColor: 0, 255, 136;
        --bodyColor: 255, 255, 255;

        --themeColorGreen: 0, 255, 136;
        --themeColorCyan: 0, 246, 255;
        --themeColorOrange: 255, 128, 0;
        --themeColorYellow: 255, 247, 0;
        --themeColorWhite: 255, 255, 255;
    }

    [data-theme='cyan'] {
        --backgroundColor: 6, 6, 6;
        --primaryColor: 0, 246, 255;
        --bodyColor: 255, 255, 255;
    }

    [data-theme='yellow'] {
        --backgroundColor: 6, 6, 6;
        --primaryColor: 255, 247, 0;
        --bodyColor: 255, 255, 255;
    }

    [data-theme='orange'] {
        --backgroundColor: 6, 6, 6;
        --primaryColor: 255, 128, 0;
        --bodyColor: 255, 255, 255;
    }

    [data-theme='white'] {
        --backgroundColor: 6, 6, 6;
        --primaryColor: 255, 255, 255;
        --bodyColor: 255, 255, 255;
    }

    html {
        background: rgb(var(--backgroundColor));
        font-size: 10px;
        padding: 30px;

        @media (max-width: 1000px) {
            padding: 20px;
        }
    }

    body,
    button,
    input,
    textarea {
        font-family: 'MartianMono', monospace;
        font-size: 1.5rem;
    }

    #root {
        background: rgba(var(--primaryColor), 0.04);
        border: 2px solid rgb(var(--primaryColor));
        color: rgb(var(--bodyColor));
        height: 100%;
        display: flex;
        flex-direction: column;

        &.crt-active {
            &:before {
                content: '';
                display: block;
                position: absolute;
                top: 0;
                left: 0;
                bottom: 0;
                right: 0;
                background: linear-gradient(
                        rgba(18, 16, 16, 0) 50%,
                        rgba(0, 0, 0, 0.25) 50%
                    ),
                    linear-gradient(
                        90deg,
                        rgba(255, 0, 0, 0.06),
                        rgba(0, 255, 0, 0.02),
                        rgba(0, 0, 255, 0.06)
                    );
                background-size: 100% 2px, 3px 100%;
                pointer-events: none;
                z-index: 10;
            }
        }
    }

    h1 {
        color: rgb(var(--primaryColor));
        font-size: 3rem;
        margin-bottom: 20px;
    }
    h2 {
        color: rgb(var(--primaryColor));
        font-size: 2.6rem;
        margin-bottom: 20px;
        margin-top: 40px;
    }
    h3 {
        display: block;
        color: rgb(var(--primaryColor));
        font-size: 2.2rem;
        margin-bottom: 20px;
        margin-top: 40px;
    }
    h4 {
        color: rgb(var(--primaryColor));
        font-size: 1.8rem;
        margin-bottom: 10px;
        margin-top: 40px;
    }
    h5 {
        color: rgb(var(--primaryColor));
        font-size: 1.6rem;
        text-transform: uppercase;
        margin-bottom: 10px;
        margin-top: 40px;
    }
    h6 {
        color: rgb(var(--primaryColor));
        font-size: 1.4rem;
        text-transform: uppercase;
        margin-bottom: 10px;
        margin-top: 40px;
    }
    time {
        display: block;
        opacity: 0.7;
    }
    p {
        margin-bottom: 20px;
        line-height: 2.4rem;
    }
    a {
        text-decoration: none;
        border-bottom: 2px solid rgb(var(--primaryColor));
        color: rgb(var(--bodyColor));

        &:hover {
            background-color: rgb(var(--primaryColor));
            color: rgb(var(--backgroundColor));
        }
    }
    ul,
    ol {
        margin-bottom: 20px;
    }
    ul li {
        list-style-type: square;
    }
    li {
        line-height: 2.4rem;
    }
    hr {
        border: none;
        border-bottom: 2px solid rgb(var(--primaryColor));
        margin: 30px 0;
    }

    /* custom scrollbar */
    ::-webkit-scrollbar {
        width: 20px;
    }
    ::-webkit-scrollbar-track {
        background-color: transparent;
    }
    ::-webkit-scrollbar-thumb {
        background-color: rgb(var(--primaryColor));
        border: 6px solid transparent;
        background-clip: content-box;
        min-height: 40px;
    }
    ::-webkit-scrollbar-thumb:hover {
        background-color: rgb(var(--primaryColor));
    }
    ::selection {
        background: rgb(var(--primaryColor));
        color: rgb(var(--backgroundColor));
    }
    ::-moz-selection {
        background: rgb(var(--primaryColor));
        color: rgb(var(--backgroundColor));
    }
</style>
